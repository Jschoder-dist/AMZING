[project]
name = "maze-generator"
version = "0.1.0"
description = "Standalone maze generator library for A-Maze-ing"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
